name: Build Android APK

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Generate Fake Debug Keystore & Build APK
        run: |
          # گریڈل فائل بنانا اور پرمیشن دینا
          gradle wrapper
          chmod +x gradlew
          
          # کلاؤڈ کمپیوٹر کے اندر خود سے debug.keystore فائل جنریٹ کرنا تاکہ ایرر ختم ہو جائے
          keytool -genkey -v -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"
          
          # اگر پراجیکٹ کے اندرونی فولڈر میں فائل چاہیے تو وہاں کاپی کرنا
          cp debug.keystore app/ || true
          
          # اب فائنل بلڈ رن کرنا
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Smart-Timetable-APK
          path: app/build/outputs/apk/debug/app-debug.apk
          
